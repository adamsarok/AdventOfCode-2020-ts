// import { type Vec, getNeighbourVecs } from '../../types/Vec';

// const first = (input: string) => {
//   const seats = input.split('\n').map((x) => x.trim().split(''));
//   let iterations = 0;
//   let state = -1;
//   while (!iterate(seats, -1)) {
//     iterations++;
//   }
//   // is it enough to count num of seats to determine stable state?
//   return iterations;
// };

// const expectedFirstSolution = 37;

// const second = (input: string) => {
//   return 'solution 2';
// };

// const expectedSecondSolution = 'solution 2';

// export { first, expectedFirstSolution, second, expectedSecondSolution };

// function iterate(seats: string[][]) {
//   for (let x = 0; x < seats.length; x++) {
//     for (let y = 0; y < seats[x].length; y++) {}
//   }
// }

// const neighbors = getNeighbourVecs();

// function getNeighbours(x: number, y: number) {
//   const result: string[] = [];
//   for (let n of neighbors) {
//     const xx: x + n.x;
//     const yy: y + n.y;
//     if (xx >= 0 && xx < )
//   }
// }
